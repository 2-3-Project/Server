커밋 방법

```
git checkout dev
git pull origin dev
git checkout user/이름 ( 이름에 자기 이름 쓰기. 예시 : user/박재영 )
git merge dev
git add .
git commit -m "커밋 메시지"
git push origin user/이름
```
그럴일은 없겠지만
<h2>중간에 오류나면 꼭 지수민 혹은 박재영 호출...</h2>
